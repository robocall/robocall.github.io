title: "// commentary — notes on tech"
author: jmj
url: https://robocall.github.io/
description: > # description of the site (multiple lines allowed)
  Welcome to my blog! I'm interested in creative coding, data science, and technical writing.

permalink: /:slug

favicon: "/slashlogo.png" # tab / PWA icon (root file)

# Ensure we don't implicitly load a gem theme.
theme: null

# Local theme (no remote_theme)
date_format: "%Y-%m-%d"
show_reading_time: true
show_tags: true
back_home_text: "cd .."

github_url: "https://github.com/robocall"
linkedin_url: "https://www.linkedin.com/in/jjcodes/"

highlighter: rouge

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
